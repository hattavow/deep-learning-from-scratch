# deep-learning-from-scratch

A hands-on repository for the book "ゼロから作る Deep Learning".

## Prepare Environment

```
python3 -m venv .venv
source .venv/bin/activate
pip install numpy matplotlib
```

Install GUI backend for matplotlib if needed:

```
pip install PyQt6
```
